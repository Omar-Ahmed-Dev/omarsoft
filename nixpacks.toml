
[phases.setup]
nixPkgs = ["php82", "php82Extensions.pdo", "php82Extensions.pdo_sqlite", "php82Extensions.mbstring", "php82Extensions.xml", "php82Extensions.tokenizer", "php82Extensions.curl", "php82Extensions.fileinfo", "composer"]

[start]
cmd = "php artisan serve --host=0.0.0.0 --port=$PORT"
